image: python:3.8

pipelines:
  default:
    - step:
        name: Run tests
        caches:
          - pip
        script:
          - if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          - pip install pytest
          - pytest -v
